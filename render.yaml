services:
  - type: web
    name: roblox-ban-api
    env: python
    plan: free
    buildCommand: pip install -r roblox-ban-api/requirements.txt
    startCommand: uvicorn roblox-ban-api.main:app --host 0.0.0.0 --port 10000
